I am using the OpenUSD pxr Python API to build a modular tank. Right now, my assets in the out/ folder are just basic UsdGeom primitives (Cubes and Cylinders). I want to upgrade the geometry to look like a realistic military tank (e.g., sloped armor, road wheels) entirely via Python code, without importing any external OBJ or FBX files. Please generate updated Python scripts for the following components, ensuring they output to the out/ folder and maintain the exact same USD root paths so my downstream tank_assembly.usda and rigging do not break. Task 1: Chassis (chassis.py) Replace the UsdGeom.Cube with a UsdGeom.Mesh. Write the Python code to define a custom Vt.Vec3fArray of points and Vt.IntArray of face vertex counts/indices to create a tank hull with sloped front glacis armor and angled rear armor (similar to an M1 Abrams or Leopard tank profile). Task 2: Turret (turret.py) Replace the base cylinder with a faceted UsdGeom.Mesh for the turret housing (giving it an angled, armored look). Keep the weaponType VariantSet exactly as it is, but upgrade the barrels by stacking multiple cylinders of different radii to create a muzzle brake and a thicker mantlet at the base. Task 3: Tracks (track.py) Inside the track stage, add a new procedural loop that generates a row of UsdGeom.Cylinder primitives representing the tank's road wheels and drive sprockets, placed perfectly inside the UsdGeom.PointInstancer tread link trajectory. Take it step by step, and show me the updated Python code for these three scripts.